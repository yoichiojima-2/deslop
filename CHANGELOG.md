# Changelog

## v1.0.0 — 2026-09-22

First release. Four shipped scores (`ad`, `slop`, `seo`, `derivative`) as TypeSafe `noul` questions, `clickbait` held as experimental. Runner refuses bodies under 250 characters. Eval set of 38 hand-written pages; all shipped dimensions above the 0.85 accuracy gate (see README for the table).

## v1.0.1 — 2026-09-22

Repo restructured as a Claude Code plugin: `skill/` is now `skills/deslop/`, with `.claude-plugin/plugin.json` and `marketplace.json`. No change to the questions, runner or scores.
